# Requisitos

php 8.2 o mayor

Instalar dependencias:

```sh
composer install
```
Generar llave

```sh
php artisan key:generate
```

Correr migraciones:

```sh
php artisan migrate:fresh --seed
```

Servidor de desarrollo:

```sh
php artisan serve
```
